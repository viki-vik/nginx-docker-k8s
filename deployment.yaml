apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-js-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kube-js-app
  template:
    metadata:
      labels:
        app: kube-js-app
    spec:
      containers:
      - name: kube-js-app
        image: viki8/ngnix-alpine-unp:1.02
        ports:
        - containerPort: 8080
          protocol: TCP
